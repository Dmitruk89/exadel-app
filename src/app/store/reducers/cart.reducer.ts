// TODO: clear this reducers or replace it by Cart-Feature modele's file.
// import { ECartActionTypes, CartActions } from '../actions/cart.action';
// import { initialCartState, CartState } from '../state/cart.state';

// export function CartReducers(state: CartState = initialCartState, action: CartActions): CartState {
//   switch (action.type) {
//     case ECartActionTypes.ADD_PRODUCT:
//       return {
//         ...state,
//         products: [...state.products, action.payload],
//       };
//     default:
//       return state;
//   }
// }
